{% extends 'base.html.twig' %}

{% block title %}Edit Inventory{% endblock %}

{% block main %}
    <h1>Édition de {{ inventory.category.titleJson.fr }}</h1>

    {{ include('inventory/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_inventory_index') }}">Retour à la liste</a>

    {{ include('inventory/_delete_form.html.twig') }}
{% endblock %}
