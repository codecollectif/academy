{% extends 'base.html.twig' %}

{% block main %}Chapter{% endblock %}

{% block body %}
    <h1>Chapter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chapter.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chapter_index') }}">back to list</a>

    <a href="{{ path('app_chapter_edit', {'id': chapter.id}) }}">edit</a>

    {{ include('chapter/_delete_form.html.twig') }}
{% endblock %}
